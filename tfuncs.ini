  mormot.lib.quickjs,
  function JSUX(ctx: PJSContext; this_val: JSValueRaw; argc: integer; argv: PJSValues): JSValueRaw; cdecl;
var
  comp, what, val: string;
begin
  comp := output + ctx^^.ToUtf8(vals^[0]);
  what := output + ctx^^.ToUtf8(vals^[1]);
  val := output + ctx^^.ToUtf8(vals^[2]);

  fBG,
  with
  Result := JS_UNDEFINED;
end;

