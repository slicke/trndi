unit LCLProc;

{$mode objfpc}{$H+}

interface

// Minimal stub of LCLProc to satisfy headless builds.
// The production unit (umain) lists this in its uses clause but does not
// reference any symbols from it at present. Keep this stub empty; add only
// what callers actually need.

implementation

end.
