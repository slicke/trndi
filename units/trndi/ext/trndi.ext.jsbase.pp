(*
 * Placeholder unit wrapper for trndi.ext.jsbase
 * The actual implementation is included into `trndi.ext.engine.pp` via
 * `{$I trndi.ext.jsbase.inc}`. This file provides a unit declaration so
 * tools and checks (e.g., `make check-module-names`) can find the unit.
 *)
unit trndi.ext.jsbase;

{$mode objfpc}{$H+}

interface

implementation

end.

